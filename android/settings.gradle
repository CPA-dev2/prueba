pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'PrendaCreditoAvanza'
apply from: file("../node_modules/@react-native/gradle-plugin/src/main/kotlin/com/facebook/react/settings.gradle")
applySettings(settings)
include ':app'